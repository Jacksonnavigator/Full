services:
  - type: web
    name: majiscope-community-connect
    env: node
    branch: main
    buildCommand: npm install && npm run build
    startCommand: node .output/server/index.mjs
    envVars:
      - key: NITRO_PRESET
        value: render_com
      - key: NODE_VERSION
        value: "22"
