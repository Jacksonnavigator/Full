module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/node:child_process [external] (node:child_process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:child_process", () => require("node:child_process"));

module.exports = mod;
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/node:fs [external] (node:fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:fs", () => require("node:fs"));

module.exports = mod;
}),
"[externals]/node:os [external] (node:os, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:os", () => require("node:os"));

module.exports = mod;
}),
"[externals]/node:path [external] (node:path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:path", () => require("node:path"));

module.exports = mod;
}),
"[externals]/node:util [external] (node:util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util", () => require("node:util"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[project]/app/api/utility-boundary/extract/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "POST",
    ()=>POST
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$child_process__$5b$external$5d$__$28$node$3a$child_process$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:child_process [external] (node:child_process, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$crypto__$5b$external$5d$__$28$node$3a$crypto$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:crypto [external] (node:crypto, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs__$5b$external$5d$__$28$node$3a$fs$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:fs [external] (node:fs, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$os__$5b$external$5d$__$28$node$3a$os$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:os [external] (node:os, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:path [external] (node:path, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$util__$5b$external$5d$__$28$node$3a$util$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:util [external] (node:util, cjs)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/server.js [app-route] (ecmascript)");
;
;
;
;
;
;
;
const execFileAsync = (0, __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$util__$5b$external$5d$__$28$node$3a$util$2c$__cjs$29$__["promisify"])(__TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$child_process__$5b$external$5d$__$28$node$3a$child_process$2c$__cjs$29$__["execFile"]);
const MAX_UPLOAD_BYTES = 12 * 1024 * 1024;
const ENVELOPE_BYTE_LENGTHS = [
    0,
    32,
    48,
    48,
    64
];
function jsonError(message, status = 400) {
    return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
        error: message
    }, {
        status
    });
}
function quoteIdentifier(identifier) {
    return `"${identifier.replace(/"/g, '""')}"`;
}
async function runSqlite(databasePath, query) {
    const { stdout } = await execFileAsync("sqlite3", [
        databasePath,
        query
    ], {
        maxBuffer: 16 * 1024 * 1024
    });
    return stdout.trim();
}
function parseLayerRows(output) {
    if (!output) return [];
    return output.split("\n").map((line)=>line.split("\t")).filter((parts)=>parts.length >= 4).map(([tableName, columnName, geometryTypeName, srsId])=>({
            tableName,
            columnName,
            geometryTypeName,
            srsId
        }));
}
function readUInt32(buffer, offset, littleEndian) {
    return littleEndian ? buffer.readUInt32LE(offset) : buffer.readUInt32BE(offset);
}
function readDouble(buffer, offset, littleEndian) {
    return littleEndian ? buffer.readDoubleLE(offset) : buffer.readDoubleBE(offset);
}
function coordinateDimensions(rawGeometryType) {
    if (rawGeometryType >= 3000) return 4;
    if (rawGeometryType >= 1000) return 3;
    return 2;
}
function baseGeometryType(rawGeometryType) {
    return rawGeometryType % 1000;
}
function parseWkbGeometry(buffer, startOffset) {
    let offset = startOffset;
    const littleEndian = buffer.readUInt8(offset) === 1;
    offset += 1;
    const rawGeometryType = readUInt32(buffer, offset, littleEndian);
    offset += 4;
    const geometryType = baseGeometryType(rawGeometryType);
    const dimensions = coordinateDimensions(rawGeometryType);
    const rings = [];
    if (geometryType === 3) {
        const ringCount = readUInt32(buffer, offset, littleEndian);
        offset += 4;
        for(let ringIndex = 0; ringIndex < ringCount; ringIndex += 1){
            const pointCount = readUInt32(buffer, offset, littleEndian);
            offset += 4;
            const ring = [];
            for(let pointIndex = 0; pointIndex < pointCount; pointIndex += 1){
                const longitude = readDouble(buffer, offset, littleEndian);
                const latitude = readDouble(buffer, offset + 8, littleEndian);
                offset += dimensions * 8;
                ring.push({
                    latitude,
                    longitude
                });
            }
            if (ringIndex === 0) {
                rings.push(ring);
            }
        }
    } else if (geometryType === 6) {
        const polygonCount = readUInt32(buffer, offset, littleEndian);
        offset += 4;
        for(let polygonIndex = 0; polygonIndex < polygonCount; polygonIndex += 1){
            const parsed = parseWkbGeometry(buffer, offset);
            offset = parsed.offset;
            rings.push(...parsed.rings);
        }
    } else {
        throw new Error(`Unsupported boundary geometry type: ${geometryType}`);
    }
    return {
        rings,
        offset
    };
}
function parseGeoPackageGeometry(hexGeometry) {
    const buffer = Buffer.from(hexGeometry, "hex");
    if (buffer.length < 8 || buffer.toString("ascii", 0, 2) !== "GP") {
        throw new Error("Invalid GeoPackage geometry header");
    }
    const flags = buffer.readUInt8(3);
    const envelopeIndicator = flags >> 1 & 0b111;
    const envelopeBytes = ENVELOPE_BYTE_LENGTHS[envelopeIndicator];
    if (envelopeBytes === undefined) {
        throw new Error("Unsupported GeoPackage envelope");
    }
    const wkbOffset = 8 + envelopeBytes;
    return parseWkbGeometry(buffer, wkbOffset).rings;
}
function normalizeRing(ring) {
    if (ring.length > 1) {
        const first = ring[0];
        const last = ring[ring.length - 1];
        if (first.latitude === last.latitude && first.longitude === last.longitude) {
            return ring.slice(0, -1);
        }
    }
    return ring;
}
function polygonArea(ring) {
    let area = 0;
    for(let index = 0; index < ring.length; index += 1){
        const current = ring[index];
        const next = ring[(index + 1) % ring.length];
        area += current.longitude * next.latitude - next.longitude * current.latitude;
    }
    return Math.abs(area / 2);
}
function calculateCenter(points) {
    let twiceArea = 0;
    let longitudeSum = 0;
    let latitudeSum = 0;
    for(let index = 0; index < points.length; index += 1){
        const current = points[index];
        const next = points[(index + 1) % points.length];
        const cross = current.longitude * next.latitude - next.longitude * current.latitude;
        twiceArea += cross;
        longitudeSum += (current.longitude + next.longitude) * cross;
        latitudeSum += (current.latitude + next.latitude) * cross;
    }
    if (Math.abs(twiceArea) > 1e-12) {
        return {
            latitude: latitudeSum / (3 * twiceArea),
            longitude: longitudeSum / (3 * twiceArea)
        };
    }
    return {
        latitude: points.reduce((sum, point)=>sum + point.latitude, 0) / points.length,
        longitude: points.reduce((sum, point)=>sum + point.longitude, 0) / points.length
    };
}
function selectBoundaryRing(rings) {
    return rings.map(normalizeRing).filter((ring)=>ring.length >= 3).sort((left, right)=>polygonArea(right) - polygonArea(left))[0];
}
function extractRingsFromGeoJson(input) {
    const rings = [];
    const visitGeometry = (geometry)=>{
        if (!geometry || typeof geometry !== "object") return;
        if (geometry.type === "Polygon") {
            const exterior = geometry.coordinates?.[0];
            if (Array.isArray(exterior)) {
                rings.push(exterior.filter((point)=>Array.isArray(point) && point.length >= 2).map((point)=>({
                        longitude: Number(point[0]),
                        latitude: Number(point[1])
                    })));
            }
            return;
        }
        if (geometry.type === "MultiPolygon") {
            geometry.coordinates?.forEach((polygon)=>{
                const exterior = polygon?.[0];
                if (Array.isArray(exterior)) {
                    rings.push(exterior.filter((point)=>Array.isArray(point) && point.length >= 2).map((point)=>({
                            longitude: Number(point[0]),
                            latitude: Number(point[1])
                        })));
                }
            });
            return;
        }
        if (geometry.type === "GeometryCollection") {
            geometry.geometries?.forEach(visitGeometry);
        }
    };
    const value = input;
    if (value?.type === "FeatureCollection") {
        value.features?.forEach((feature)=>visitGeometry(feature?.geometry));
    } else if (value?.type === "Feature") {
        visitGeometry(value.geometry);
    } else {
        visitGeometry(value);
    }
    return rings;
}
async function extractFromGeoPackage(filePath) {
    const layerOutput = await runSqlite(filePath, "select table_name || char(9) || column_name || char(9) || geometry_type_name || char(9) || srs_id from gpkg_geometry_columns;");
    const layers = parseLayerRows(layerOutput);
    const polygonLayer = layers.find((layer)=>layer.geometryTypeName.toUpperCase().includes("POLYGON"));
    if (!polygonLayer) {
        throw new Error("No polygon or multipolygon boundary layer was found in the GeoPackage.");
    }
    if (polygonLayer.srsId !== "4326") {
        throw new Error(`Unsupported GeoPackage coordinate system ${polygonLayer.srsId}. Please upload EPSG:4326 boundary data.`);
    }
    const geometryOutput = await runSqlite(filePath, `select hex(${quoteIdentifier(polygonLayer.columnName)}) from ${quoteIdentifier(polygonLayer.tableName)} where ${quoteIdentifier(polygonLayer.columnName)} is not null;`);
    const rings = geometryOutput.split("\n").filter(Boolean).flatMap((hexGeometry)=>parseGeoPackageGeometry(hexGeometry));
    return {
        rings,
        layerName: polygonLayer.tableName,
        geometryType: polygonLayer.geometryTypeName
    };
}
async function extractFromGeoJson(file) {
    const payload = JSON.parse(await file.text());
    return {
        rings: extractRingsFromGeoJson(payload),
        layerName: "GeoJSON",
        geometryType: "POLYGON"
    };
}
async function POST(request) {
    let temporaryPath = null;
    try {
        const formData = await request.formData();
        const file = formData.get("file");
        if (!(file instanceof File)) {
            return jsonError("Upload a boundary file to extract utility geometry.");
        }
        if (file.size <= 0) {
            return jsonError("The uploaded boundary file is empty.");
        }
        if (file.size > MAX_UPLOAD_BYTES) {
            return jsonError("The uploaded boundary file is too large. Please use a file under 12 MB.");
        }
        const extension = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].extname(file.name).toLowerCase();
        let extracted;
        if (extension === ".gpkg") {
            temporaryPath = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$path__$5b$external$5d$__$28$node$3a$path$2c$__cjs$29$__["default"].join(__TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$os__$5b$external$5d$__$28$node$3a$os$2c$__cjs$29$__["default"].tmpdir(), `utility-boundary-${(0, __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$crypto__$5b$external$5d$__$28$node$3a$crypto$2c$__cjs$29$__["randomUUID"])()}.gpkg`);
            await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs__$5b$external$5d$__$28$node$3a$fs$2c$__cjs$29$__["promises"].writeFile(temporaryPath, Buffer.from(await file.arrayBuffer()));
            extracted = await extractFromGeoPackage(temporaryPath);
        } else if (extension === ".geojson" || extension === ".json") {
            extracted = await extractFromGeoJson(file);
        } else {
            return jsonError("Unsupported boundary file. Please upload a GeoPackage (.gpkg) or GeoJSON file.");
        }
        const boundaryPoints = selectBoundaryRing(extracted.rings);
        if (!boundaryPoints || boundaryPoints.length < 3) {
            return jsonError("No usable polygon boundary points were found in the uploaded file.");
        }
        const center = calculateCenter(boundaryPoints);
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$server$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["NextResponse"].json({
            center,
            boundaryPoints,
            source: {
                fileName: file.name,
                layerName: extracted.layerName,
                geometryType: extracted.geometryType,
                pointCount: boundaryPoints.length
            }
        });
    } catch (error) {
        console.error("Utility boundary extraction failed:", error);
        return jsonError(error instanceof Error ? error.message : "Failed to extract utility boundary geometry.", 422);
    } finally{
        if (temporaryPath) {
            await __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$fs__$5b$external$5d$__$28$node$3a$fs$2c$__cjs$29$__["promises"].unlink(temporaryPath).catch(()=>undefined);
        }
    }
}
}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__4542f85b._.js.map